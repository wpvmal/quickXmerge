#ifndef BAR_CONTROL_H
#define BAR_CONTROL

#include <vector>
using namespace std;

void printSearch(int size, int line, int delay, char side);
void printBar(int size, int line, int delay, char side);
void deleteBar(int size, int line, int delay, char side);

#endif